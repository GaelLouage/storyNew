@model Infrastructuur.Models.StoryzonEntity

@{
    ViewData["Title"] = "Delete";
    var textTop = Model?.BodyEn?.Split('.').Take(8).ToList();
    var textbottom = Model?.BodyEn?.Split('.').Skip(8).ToList();
}



<h3 class="text-center mt-5 mb-5">Are you sure you want to delete @Model.Title?</h3>


<hr />

    <div class="w-100 text-center d-flex justify-content-center mt-5 mb-5">
        <img class="mobile-w-50" src="~/@Model.Image" />
    </div>


<form class="text-center" asp-action="Delete">
    <input  type="submit" value="Delete" class="btn bg-dark text-white" /> 
    <a class="btn main-dark-button w-100 mt-5" id="form-submit" class="btn main-dark-button" asp-action="AdminList">Go back</a>
</form>
